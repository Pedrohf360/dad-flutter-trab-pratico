1) Start xampp control em C:\xampp\...

2) php -S seuIpLocal:8080 -> run php server

seuIpLocal = windows: ethernet -> propriedads OU settings do wifi (Ipv4)

Obs.:
Flutter: é necessário alterar o arquivo login_page.dart, no método signIn, para o IP local atual.